import { DateLongShortPipe } from './date-long-short.pipe';

describe('DateLongShortPipe', () => {
  it('create an instance', () => {
    const pipe = new DateLongShortPipe();
    expect(pipe).toBeTruthy();
  });
});
