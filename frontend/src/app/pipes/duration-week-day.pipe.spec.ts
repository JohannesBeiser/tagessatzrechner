import { DurationWeekDayPipe } from './duration-week-day.pipe';

describe('DurationWeekDayPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationWeekDayPipe();
    expect(pipe).toBeTruthy();
  });
});
